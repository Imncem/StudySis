{
  "scripts": {
    "test": "npm run test:rules:emulator",
    "test:rules": "node --test test/firestore-rules/*.test.js",
    "test:rules:emulator": "firebase emulators:exec --only firestore \"npm run test:rules\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0"
  }
}
